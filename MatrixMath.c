// Source code for matrix multiplication in C without using function
#include <stdio.h>

int main()
{
    int m, n, p, q, c, d, k, sum = 0;
    int first[10][10], second[10][10], multiply[10][10];

    printf("\nEnter the numbers of row and column of first matrix");
    scanf("%d%d", &m, &n);
    
}